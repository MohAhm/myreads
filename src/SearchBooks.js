import React, { Component } from 'react';

class SearchBooks extends Component {
    state = {  }

    render() {
        return (
            <div>Search Page</div>
         );
    }
}

export default SearchBooks;